# approval-gate

Human approval gate CLI for high-risk agent actions.

## Install

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Usage

```bash
approval-gate --task "demo"
```
